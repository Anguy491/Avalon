# M5 移动端验收证据

本目录中的截图由 `pnpm test:e2e:mobile` 的 M5 Maestro 流程生成。测试仅使用确定性本地夹具，不包含真实令牌、生产账号或可复用秘密。

- `ios-assassination-*.png`：刺杀人选与提交前二次确认；
- `ios-role-guide-*.png`：仅按本人私密角色展示的可滚动攻略弹窗；
- `ios-result-*.png`：终局胜方、原因、刺杀目标与全员角色公开；
- `large`：iOS 最大辅助动态字体；`normal`：iOS 默认动态字体。
